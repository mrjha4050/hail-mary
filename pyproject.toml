[project]
name = "assign"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "celery>=5.6.3",
    "django>=6.0.7",
    "django-silk>=5.5.0",
    "django-unfold>=0.100.0",
    "loguru>=0.7.3",
    "redis>=8.0.1",
]
